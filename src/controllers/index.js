import {
    BarController,
    HorizontalBarController,
} from './controller.bar';
import { BubbleController } from './controller.bubble';
import {
    DoughnutController,
    PieController,
} from './controller.doughnut';
import {
    LineController,
    ScatterController,
} from './controller.line';
import { PolarAreaController } from './controller.polarArea';
import { RadarController } from './controller.radar';

export const bar = BarController;
export const bubble = BubbleController;
export const doughnut = DoughnutController;
export const pie = PieController;
export const line = LineController;
export const scatter = ScatterController;
export const polar = PolarAreaController;
export const radar = RadarController;
